"""Project-wide constants."""

APP_NAME = "ReconFlow"
APP_VERSION = "0.1.0"
AUTHORIZATION_WARNING = (
    "Authorization required: run scans only against targets you own or are"
    " explicitly permitted to test."
)
